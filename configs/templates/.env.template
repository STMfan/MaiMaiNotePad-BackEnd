# ============================================================================
# 环境配置文件 (.env)
# 用途：敏感信息、环境差异配置
# 注意：此文件不应提交到版本控制
# ============================================================================

# -------------------- 密钥和密码（必需） --------------------
# JWT 密钥（必需，生产环境必须设置强随机密钥）
JWT_SECRET_KEY=change_me_to_a_very_long_random_string_32_chars_or_more

# 超级管理员密码（必需，仅在数据库为空时用于初始化）
SUPERADMIN_PWD=admin123456

# 最高权限密码（必需，用于敏感操作，例如提权为管理员/版主）
HIGHEST_PASSWORD=your_highest_password

# 邮箱授权码（必需，SMTP 密码或授权码）
MAIL_PWD=your_email_authorization_code

# -------------------- 环境差异配置 --------------------
# 数据库连接（不同环境使用不同数据库）
DATABASE_URL=sqlite:///data/mainnp.db
# 开发环境: sqlite:///data/mainnp.db
# 测试环境: postgresql://test:test@localhost:5432/maimnp_test
# 生产环境: postgresql://user:pass@db-host:5432/maimnp

# 服务器配置（不同环境可能不同）
HOST=0.0.0.0
PORT=9278

# 邮箱配置（不同环境可能使用不同邮箱）
MAIL_HOST=smtp.qq.com
MAIL_USER=your_email@qq.com
MAIL_PORT=465

# 日志级别（开发环境 DEBUG，生产环境 INFO/WARNING）
LOG_LEVEL=INFO

# -------------------- 可选配置 --------------------
# 外部域名（用于构造邮件链接等）
EXTERNAL_DOMAIN=example.com

# 上传目录（测试环境可能使用不同目录）
UPLOAD_DIR=uploads

# AI 内容审核配置（硅基流动）
# 请在 https://cloud.siliconflow.cn/ 获取 API Key
SILICONFLOW_API_KEY=your_api_key_here

# -------------------- 测试环境特殊配置 --------------------
# 测试环境降低 bcrypt rounds 加速测试（仅测试环境使用）
# PASSLIB_BCRYPT_ROUNDS=4
