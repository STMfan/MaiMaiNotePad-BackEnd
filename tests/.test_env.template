# 测试环境配置模板

# 数据库配置（在 tests 目录内）
DATABASE_URL=sqlite:///./tests/test.db

# 上传目录配置（测试专用目录）
UPLOAD_DIR=test_uploads

# JWT 配置（测试专用密钥）
JWT_SECRET_KEY=test_jwt_secret_key_for_testing_only

# 密码哈希配置（使用较少的 rounds 加速测试）
PASSLIB_BCRYPT_ROUNDS=4

# 测试配置
HYPOTHESIS_PROFILE=ci
TEST_PARALLEL=true
